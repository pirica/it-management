# AGENT_NOTES.md - integration_accounts

External-style chart rows (`nominal_code`, balances, optional `gl_account_id` bridge to internal `gl_accounts`). Used on bill/invoice line `integration_account_id`.
