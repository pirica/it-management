# AGENT_NOTES — modules/ticket_canned_responses

Tenant-scoped canned comment snippets (`title`, `body`, optional `category_id`). Flattened CRUD; not in record-share slugs. Picker: Shift+F2 in ticket comments. Regression: `php scripts/verify_ticket_productivity.php`. Doc: `docs/TICKET_PRODUCTIVITY.md`.
