# AGENT_NOTES.md - booking_rooms_types includes

## 1. Module Purpose

Shared FK helpers for the Room Types CRUD module (`modules/booking_rooms_types/`).

## 7. File Structure

- **brt_fk_helpers.php** — `upgrade_to_room_type_id` list/view/form options resolve **`booking_rooms_types.code`** (not `name`).
