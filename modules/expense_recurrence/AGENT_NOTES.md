# AGENT_NOTES.md - expense_recurrence

Tenant lookup for recurring expense intervals (`code` + `sort_order`). Referenced by `expenses.expense_recurrence_id`. Runner: `php scripts/run_expense_recurrence.php`.
