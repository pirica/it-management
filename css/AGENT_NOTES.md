# AGENT_NOTES.md - CSS

## 1. Module Purpose
Contains global and module-specific stylesheets (CSS) for the IT Management system.

## 7. File Structure
- **styles.css** — the main application stylesheet.
- **theme.css** — handles dark/light mode variables.

## 10. Common Pitfalls
- Overwriting global variables without checking their impact on other modules.

## 12. Module Owner Notes (Optional)
The system uses CSS variables (`var(--accent)`, etc.) for theme consistency.
