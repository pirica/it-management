# AGENT_NOTES.md - Includes

## 1. Module Purpose
Contains shared PHP logic, helper functions, and visibility filters used across multiple modules.

## 2. Key Tables
- Primarily provides helper logic for **alerts**, **equipment**, and **audit_logs**.

## 3. Required Relationships
- Functions here often depend on `config/config.php`.

## 4. Business Rules (Critical for Agents)
- **Visibility Helpers**: `alerts_visibility.php` is mandatory for all alert-related queries.
- **Security Functions**: Use `itm_is_safe_identifier` for dynamic SQL identifiers.

## 7. File Structure
- **bootstrap_helpers.php** — shared bootstrap utilities including `itm_mysqli_connect()` (MySQL port / `host:port` parsing for `.env` `DB_PORT`) and `itm_release_session_lock()` (SSE / long-poll APIs release the PHP session write lock so other tabs load immediately).
- **itm_cli_binary.php** — `itm_resolve_cli_php_binary()` / `itm_resolve_cli_mysql_binary()` (`.env` `PHP_EXE`, `MYSQL_EXE`); `itm_resolve_phpunit_cli_binary()` picks the first CLI `php.exe` with PHPUnit extensions (Dunebox under `D:\dunebox-v1.0.6`, Laragon portable sibling, or `PHP_BINARY`).
- **alerts_visibility.php** — centralized visibility logic for global/private alerts.
- **itm_finance_attachments.php** — finance module file attachments (multi-upload, `finance/{company_id}/{module}/{document_key}/`, 5 MB, allowed extensions); loaded from `config/config.php`.
- **notes_visibility.php** — owner + `shared_with_json` filter for Notes module; `itm_notes_fetch_visible_by_id()` for single-record view/edit load; `itm_notes_private_images_dir()`, `itm_notes_normalize_image_filename()`, and `itm_notes_resolve_image_path()` confine note attachment ZIP downloads to `files/{company_id}/Private/{username}_{employee_id}/notes/`; `itm_notes_json_mutation_response()` standardises AJAX mutation JSON (404 when zero rows affected).
- **events_visibility.php** — owner + `shared_with_json` filter for Events module; `itm_events_fetch_visible_by_id()` for single-record view/edit/export load.
- **itm_appointment.php** — tenant appointment settings, business hours, visit reasons, weekly slot grid (`itm_appointment_build_week_slots()` with past-slot and inactive-settings gates), self-service helpers (`itm_appointment_employee_can_modify()`, `appt_employee_can_modify()`), ICS builder (`itm_appointment_build_ics_vevent()`), confirmation email (`itm_appointment_send_confirmation_email()`), assignee cancel notify (`itm_appointment_notify_assignee_cancelled()`). Used by `modules/appointments/`.
- **itm_hotel_booking.php** — hospitality segment/status helpers, planning grid (one hotel per company; Arrivals/Departures/In-house/Future + hide statuses; CANCELLED/NO-SHOW on empty row), **`hotel_booking_last_rooms`** snapshot upsert, overlap/cancelled checks, portal customer ensure (**`itm_hotel_booking_ensure_customer_for_portal()`** updates `customers.name` / `phone` on repeat book by email), guest manage lookup (`itm_hotel_booking_fetch_for_guest_manage` by **`guest_confirmation_code`** + last name + **`auth2`** 12-char code only — legacy 4-digit stored PINs retired; `itm_hotel_booking_portal_backfill_legacy_auth2_groups()`), **opaque guest confirmation code** (`itm_hotel_booking_generate_guest_confirmation_code`, `itm_hotel_booking_portal_confirmation_primary_guest_code`), **manage lookup rate limit** (`itm_hotel_booking_portal_manage_rate_limit_check` / `_record`) and **OTP verify rate limit** (`itm_hotel_booking_portal_manage_otp_rate_limit_*`), **generic manage failure copy** (`itm_hotel_booking_portal_manage_lookup_failure_message`), **locked portal INSERT** (`itm_hotel_booking_portal_insert_booking_locked` — room `FOR UPDATE` + overlap + INSERT; optional `nested` for multi-room outer transaction), **Step 4 charge** (`itm_hotel_booking_portal_resolve_step4_charge`; multi-room `itm_hotel_booking_portal_resolve_room_lines_pricing_from_db`), **portal step pricing** (`itm_hotel_booking_portal_pricing_defaults`, `itm_hotel_booking_portal_hotel_pricing`, `itm_hotel_booking_portal_save_hotel_pricing`, `itm_hotel_booking_portal_quote_nightly`), **tax-inclusive cheapest-rate calendar** (`itm_hotel_booking_hotel_calendar_month` day `price` = cheapest plan × BAR + tourist tax via `itm_hotel_booking_portal_cheapest_rate_offer_for_hotel`; `prices_include_tax`), **Step 2 rate-plan offers** (`itm_hotel_booking_portal_rate_plan_offer` merges DB merchandising including `plan_surcharge_percent`; `itm_hotel_booking_portal_free_cancellation_days_from_settings`; `itm_hotel_booking_portal_calendar_month_advance_days_left_from_settings`; `itm_hotel_booking_portal_show_discount_strikethrough_from_settings`; `itm_hotel_booking_portal_rate_plan_effective_discount`; `itm_hotel_booking_portal_rate_plan_effective_surcharge`; `itm_hotel_booking_portal_quote_nightly` applies discount then surcharge), **room-type calendar** (`itm_hotel_booking_resolve_room_type_nightly_bar`, `itm_hotel_booking_room_type_blocked_for_stay`, `itm_hotel_booking_room_unavailable_for_stay`, `itm_hotel_booking_compute_stay_payment_dated_rates`), **multi-room portal checkout** (`itm_hotel_booking_portal_room_line_pick`, draft `room_lines`, `itm_hotel_booking_portal_insert_stay_bookings_locked` — N `hotel_bookings` rows, shared `auth2`, one transaction for multi-room), multi-photo upload paths under `booking/images/{hotel_id}/hotel_photos`, `room_types_photos`, and `room_photos`; **`itm_hotel_booking_room_duplicate_record()`** clones `hotel_booking_rooms` with suffixed `room_number` / `name`; **`itm_hotel_booking_normalize_cancellation_policy_url()`** allowlists relative policy paths to `.html` / `.htm` / `.txt` (blocks `.php` RCE writes under `booking/cancellation_policy/`); **room-type portal rules** (`itm_hotel_booking_portal_room_type_effective_rules`, `validate_stay`, `room_type_card_available`, `portal_room_type_occupancy_slice_fits`, `portal_effective_max_total_guests`, `portal_child_age_counters_valid`, `portal_connecting_unit_*`, `portal_checkout_required_room_line_count`, `complimentary_room_credit`, `draft_pet_policy`, `draft_requires_approval`, `type_row_from_joined_sql`, **`confirmation_email_flags_from_settings`**); **internal rate codes** (`itm_hotel_booking_internal_rate_*`, `itm_hotel_booking_apply_internal_rate_to_breakdown`, `itm_hotel_booking_portal_parse_internal_rate_code`); **portal date/time display** (`itm_hotel_booking_portal_format_date_display`, `itm_hotel_booking_portal_format_datetime_display`, `itm_hotel_booking_portal_public_settings_for_js`); loaded from `config/config.php`. Regression: `php scripts/verify_hotel_booking.php` and `php scripts/run_tests.php --filter HotelBookingRoomTypePortalRules`.
- **itm_hospitality_admin_layout.php** — `itm_hospitality_admin_layout_begin()` / `itm_hospitality_admin_layout_end($extraScripts, $htmlBeforeScripts)` shell (DOCTYPE, sidebar, header, `theme.js`) for bespoke hospitality admin pages; optional HTML before scripts (body-level modals outside `.content` overflow); replaces non-existent `includes/footer.php`. `extraScripts` are emitted as-is: module-relative `js/foo.js` resolves under the current module folder; repo-root files such as `js/hotel-date-input.js` must use `BASE_URL . 'js/hotel-date-input.js'`.
- **itm_maintenance_script_admin_gate.php** — `itm_enforce_maintenance_script_admin_browser()` restricts browser access to maintenance/security scripts to Admin users (CLI unchanged). Uses `itm_script_session_or_authorization_is_admin()` so disposable browser test sessions still honor the signed-in Administrator.
- **todo_visibility.php** — global/assigned/creator filter for Todo module.
- **delete_functions.php** — shared logic for complex deletions (e.g., equipment).
- **companies_view_redirect.php** — legacy company view redirect; guarded via `itm_script_entry_guard.php`.
- **get_ports.php** / **update_port.php** — switch port AJAX endpoints; guarded + shared helpers in **switch_port_api_helpers.php** (lookup maps and VLAN lists use `itm_mysqli_stmt_fetch_all_assoc()`); all JSON exits use `itm_api_json_response()` with `JSON_UNESCAPED_UNICODE`; prepared-statement reads use `itm_mysqli_stmt_fetch_assoc()` / `itm_mysqli_stmt_fetch_all_assoc()` (mysqlnd fallback). Tenant `$company_id` cast to `(int)` after `config.php`, from session only (HTTP `403` when missing; client payload `company_id` unset from `$input`/`$_POST`/`$_REQUEST`). **`update_port.php`** wraps `switch_ports` UPDATE plus To IDF auto-sync (`idf_ports` INSERT/UPDATE/DELETE) in a single transaction when `management_id` column exists; rolls back on zero-row update, IDF sync failure, or empty-position 422. Zero-row contract regression: `php scripts/verify_update_port_zero_row.php` (subprocess parses JSON wrapper after CLI `header()` lines).
- **switch_port_api_helpers.php** — shared lookup/VLAN helpers for port AJAX endpoints; tenant-scoped prepared queries use mysqlnd-safe fetch helpers.
- **itm_script_entry_guard.php** — `itm_skip_http_entry_unless_direct()`, `itm_skip_view_partial_unless_context()`, PHPUnit processing detection. **`ITM_HTTP_ENDPOINT_CONTRACT_TEST` is evaluated before `itm_is_phpunit_processing()`** so trusted CLI contract subprocesses (and `ItmScriptEntryGuardTest`) can include HTTP endpoints under CLI without the PHPUnit bare-include guard winning. **Threat model:** HTTP JSON endpoints normally no-op under CLI unless the request is the direct Apache entry script. Trusted regression subprocesses may define **`ITM_HTTP_ENDPOINT_CONTRACT_TEST`** before including an endpoint (for example `scripts/verify_update_port_zero_row.php`); that constant must **never** be derived from `$_GET`/`$_POST`/request headers or any other HTTP input — only hard-coded in vetted CLI scripts under `scripts/`.
- **switch_port_api_helpers.php** — shared lookup/VLAN helpers for port AJAX endpoints (avoids redeclare fatals during coverage).
- **itm_select_options_policy.php** — whitelist and blocked-table policy for `modules/select_options_api.php` quick-add inserts; includes **`license_types`** for License Management Type dropdown quick-add; **`companies`** is blocked (use Companies CRUD module instead).
- **itm_ui_action_labels.php** — canonical emoji-only labels for View/Edit/Delete/Back/Create/Save (`itm_ui_action_emoji()`, `itm_ui_action_title()`) and list pagination (`itm_ui_pagination_emoji()`, `itm_ui_pagination_title()` — ⏮️/◀️/▶️/⏭️); NO MIXED contract patterns for `scripts/check_ui_action_emoji.php`; full four-icon list pagination gate: `scripts/check_pagination_emoji.php` + `itm_check_pagination_nav_titles()` in `scripts/lib/itm_ui_list_contract_checks.php`. Loaded from `config/config.php`; JS mirror: `js/itm-ui-action-labels.js` (included from `header.php`).
- **itm_database_sql_source.php** — canonical paths and readers for `db/01_schema.sql`, `db/02_data.sql`, `db/02_data_sample.sql`, `db/03_triggers.sql`; `ITM_SAMPLE_SQL_TEMPLATE_COMPANY_ID` (file parse marker); loaded from `config/config.php`.
- **itm_database_migrations.php** — migration runner helpers; **live DB probe is authoritative** for Applied/Pending (`itm_database_migrations_schema_satisfied()` → `scripts/lib/itm_verify_db_migrations_report.php`); `schema_migrations` stores apply audit + checksum drift only. `itm_database_migrations_resolve_discovered_file()` validates runner-scoped `*.sql` paths; `itm_database_migrations_resolve_file_row()` also resolves bootstrap files for read-only SQL view. `itm_database_migrations_record_bootstrap_audit_rows()` records `schema_migrations.sql` in audit history when the table exists. `itm_database_migrations_delete_discovered_file()` removes a discovered file from disk and clears matching `schema_migrations` rows (Admin browser POST). `itm_database_migrations_delete_audit_row_by_id()` removes one audit history row without changing live schema (`modules/schema_migrations/delete.php`). Bootstrap DDL read from `db/migrations/schema_migrations.sql`. CLI/browser: `scripts/migrate.php`.
- **itm_sample_data_seed.php** — Add sample data for any tenant: … **`itm_seed_copy_finance_lookup_rows_from_template_company()`** copies full `tax_rates` / `paid_statuses` / `payment_modes` / `expense_recurrence` catalogs from template company 1 (same as `db/02_data.sql`) before falling back to `db/02_data_sample.sql`. **`itm_seed_apply_finance_row_defaults()`** / **`itm_seed_finance_currency_sql_literal()`** (EUR `char(3)` for `expenses`, `bills`, `invoices`, `bank_accounts`; backfill `posting_date`, `paid_status_id`); finance prerequisite map includes `paid_statuses`, `tax_rates`, `payment_modes`, and AP/AR parent chains; … Parent auto-seed uses **`itm_sample_data_prerequisite_map()` only** (not every outbound FK from schema — avoids `employees` / `companies` cascade hangs). **`itm_seed_lookup_parent_auto_seed_skip_tables()`** skips bulk seed for `companies`, `employees`, `audit_logs`, `employee_companies`, `ui_configuration` (FK remap at insert). Skips parents that already have tenant rows; extended prerequisites include `note_labels` → `notes`, `monthly_budgets` → `annual_budgets`, `patches_updates` → `equipment` + status/level lookups, `floor_designer` / `floor_designer_points`, IDF chain (`idf_positions` → `idfs` + `idf_device_type`; `idf_ports` → `idf_positions`; `idf_links` → `idf_ports`), all seven `ops_report_*` child tables → `ops_report`, and `ops_report_*` children → `ops_report`. **`itm_seed_normalize_template_value_token()`** shortens overlong `db/02_data_sample.sql` placeholders (e.g. `file_ext`, `case_closed`, IDF `equipment_id`). Template inserts auto-append **`company_id`** when the table has the column but the sample row omits it (e.g. `attempts`). Equipment lookup helpers (`equipment_types`, `equipment_statuses`, `equipment_rj45`, `switch_status`, `cable_colors`) resolve new row ids with **tenant-scoped SELECT after `itm_run_query()`** — do not trust `mysqli_insert_id()` when PHP audit logging runs (`itm_log_audit` overwrites the connection insert id). **`equipment`** uses **`itm_seed_insert_equipment_sample_rows()`** (Server + Switch with 24 RJ45 `switch_ports`). **`backup_tape_log`** uses **`itm_seed_insert_backup_tape_log_today_row()`** after **`itm_seed_ensure_server_equipment()`** (Server only). **`switch_ports`** uses **`itm_seed_ensure_switch_equipment()`** + **`itm_seed_ensure_switch_rj45_ports()`** (Core Switch, `equipment_rj45` **24 ports**, 24 RJ45 `switch_ports` rows). **`floor_plans`** uses **`itm_seed_insert_floor_plans_sample_rows()`** (`fp_seed_sample_folders_and_tags()` + placeholder file row). **`idf_positions` / `idf_ports` / `idf_links`** use dedicated minimal chain inserters when generic SQL templates are unsafe. **`approvals`** uses **`itm_seed_insert_approvals_sample_row()`** (tenant-scoped `forecast_revision_id` / `stage` / `status` — not template ids from `db/02_data.sql`). **`events`** uses **`itm_seed_insert_events_sample_rows()`** (visibility-scoped empty gate + owner `employee_id` stamp). **`employee_notifications`** uses **`itm_seed_insert_employee_notifications_sample_rows()`** (inbox-scoped empty gate + session `employee_id` stamp; tenant `record_id` / `action_url` from `module_slug`; prerequisites: `tickets`, `alerts`, `todo`, `appointments`, `events`). **`problem_ticket_links`** prerequisites: `problems`, `tickets`; **`problems`**: `knowledge_base`, `master_tickets` (sample row keeps `master_ticket_id` NULL when no master ticket exists). **`ui_configuration`** uses **`itm_seed_insert_ui_configuration_sample_row()`** with **`itm_seed_resolve_tenant_seed_admin_employee_id()`** (tenant seed Admin, then signed-in employee with **`itm_employee_has_company_access()`**, then first live tenant employee — `02_data.sql` defaults, no generic fallback). **`role_module_permissions`** uses **`itm_seed_insert_role_module_permissions_sample_rows()`** after **`itm_seed_ensure_tenant_employee_roles_for_rbac()`** (`Admin` → `ALL`, `Helpdesk`/`User` → `Tickets`). **`tickets`** uses **`itm_seed_insert_tickets_sample_row()`** (`TCK-0001`, `is_archived = 0`, session or cross-company employee when the tenant has no local employees) with **`itm_seed_ensure_tickets_lookup_parents()`** for canonical category/status/priority rows when generic fallback lookup rows already exist; **`itm_seed_sync_mysql_audit_session_for_company()`** re-stamps `@app_company_id` to the target tenant before lookup/ticket INSERT. Regression: `php scripts/verify_company_empty_sample_data.php --company=N`; `php scripts/verify_sample_data_seed.php` (CLI + Admin browser); `php scripts/verify_finance_sample_data_seed.php`; `php scripts/verify_tickets_sample_data.php`.
- **itm_sample_sql_export.php** — builds `db/02_data_sample.sql`: parses company `1` rows from `db/02_data.sql`, backfills from live MySQL, synthesizes minimal marker rows when empty; `itm_sample_sql_collect_table_templates()` used by `scripts/extract_02_data_sample.php` and `scripts/check_02_data_sample_coverage.php`. **`itm_sample_sql_exempt_tables()`** excludes matrix/junction/child/runtime tables (e.g. `company_module_share`, `finance_payment_allocations`, live chat, ticket activity/comments, webmail reads/signatures, **`search_index`** derived palette cache) from mandatory templates.
- **itm_employee_notifications.php** — per-employee inbox (`employee_notifications`): `itm_notify_employee()` dispatcher, specialized emitters (ticket, todo, events, alerts, appointment, notes, email, live chat conversation assign, ticket comment `@mention`), unread count, list recent, `itm_employee_notification_mark_read()` / `itm_employee_notification_mark_all_read()`, digest email (`itm_employee_notifications_send_digest_emails()`), optional SSE stream (`itm_employee_notifications_sse_stream()` — not auto-started in header JS); header bell uses `modules/notifications/api.php` + `js/notifications.js` (deferred `count_only` poll). Doc: `docs/NOTIFICATIONS.md`. Regression: `php scripts/verify_employee_notifications.php`.
- **itm_automation_rules.php** — workflow automation: `itm_automation_rules_dispatch()`, `itm_automation_rules_run_rule()`, `itm_automation_rules_run_scheduled()`, ticket context builders; hooks from `modules/tickets/create.php`; cron `scripts/run_automation_rules.php`. Doc: `docs/AUTOMATION_RULES.md`. Regression: `php scripts/verify_automation_rules.php`.
- **itm_saved_reports.php** — saved list views (custom report builder): `itm_saved_reports_module_config()` filter/column whitelist, `itm_saved_reports_validate_filters()`, `itm_saved_reports_save()` / `soft_delete()`, `itm_saved_reports_run_query()` (delegates to tickets/equipment/expenses list query helpers), export/share/schedule slug helpers, `itm_saved_reports_dashboard_snapshot()`. UI: `itm_saved_reports_ui.php`, `itm_saved_reports_hub_ui.php`, `itm_saved_reports_list_banner.php`. API: `modules/saved_report_views/api.php`. Doc: `docs/SAVED_REPORT_VIEWS.md`. Regression: `php scripts/verify_saved_report_views.php`.
- **itm_tickets_list_query.php** — shared tickets index filter parser + SQL runner (`itm_tickets_list_parse_filters()`, `itm_tickets_list_run_query()`); used by `modules/tickets/index.php` and saved report views.
- **itm_equipment_list_query.php** — shared equipment index filter parser + SQL runner; used by `modules/equipment/index.php` and saved report views.
- **itm_expenses_list_query.php** — shared expenses index filter parser + SQL runner; used by `modules/expenses/index.php` and saved report views.
- **itm_scheduled_reports.php** — executive report catalog, cron matcher, dataset loaders; merges owner saved views via `itm_scheduled_reports_catalog_with_saved_views()` and `saved_view:{id}` slug handling in `itm_scheduled_reports_process_due()`. Doc: `docs/SCHEDULED_REPORTS.md`.
- **itm_approval_inbox.php** — unified approval queue (`approval_inbox_items`): adapter registry (`request_password`, `employee_onboarding_requests`), `itm_approval_inbox_upsert()`, fetch/count for assignees, `itm_approval_inbox_decide()` proxy to source modules. Loaded from `config/config.php`. Doc: `docs/APPROVAL_INBOX.md`. Regression: `php scripts/verify_approval_inbox.php`.
- **itm_qr_share.php** — unified `share_sessions` CRUD (`module_slug`, `record_id`, explorer `scope_path`); token/code lookup, TTL purge. **Join hardening:** eight-digit codes for modules that allow numeric join; vault/file modules (`passwords`, `private_contacts`, `explorer`, `notes`, `webmail`) are **token-link only** (no numeric join); public join POSTs rate-limited per client IP under `files/rate_limits/qr_share_join/`. Used by Notes, Passwords, Bookmarks, Todo, Events, Private Contacts, Explorer, Floor Plans, Rack Planner, and CRUD record modules (see `itm_crud_record_share.php`). Regression: `php scripts/verify_qr_share_modules.php`, `php scripts/verify_qr_share_join_hardening.php`.
- **itm_crud_record_share.php** — shared QR/6-digit share for flattened CRUD and bespoke record views (`employees`, `tickets`, `alerts`, `equipment` record + Switch Port Manager `switch_ports`, `ops_report`); `itm_crud_record_share_handle_ajax_request()`, `itm_crud_record_share_render_action_buttons()`, `join.php` renderer via `itm_crud_record_share_render_join_page()`. Wiring helper: `php scripts/apply_crud_record_share_modules.php --apply`. Canonical inventory: **`docs/CRUD_RECORD_SHARE.md`**.
- **itm_module_share.php** — `company_module_share` matrix helpers (`has_module_share_access()`, `itm_set_company_module_share()`); enforced in `itm_qr_share_create_session()`. Admin UI: `modules/share_modules/`.
- **itm_qr_share_join.php** — shared public `join.php` HTML renderer for password, bookmark, todo, event, private contact, explorer, floor plan, rack planner, `crud_record`, `equipment_switch_ports`, and `ops_report` payload types (`itm_qr_share_render_join_page()`).
- **itm_qr_generator.php** — employee QR library (`modules/qr/`): type catalog, static/dynamic payload builders, public `r.php` resolver helpers, scan recording, design template CRUD (`qr_design_templates`). Separate from `itm_qr_share.php`.
- **itm_short_url.php** — employee short-link library (`modules/short-url/`): validation, public `go.php` URL builder (`public_base_url` per company or `BASE_URL` default), click analytics, password gate, settings load/save, linked QR scan count (`itm_short_url_fetch_linked_qr_scan_count()`), `itm_short_url_create_linked_qr()` and `itm_short_url_create_from_destination()` for bidirectional QR integration. Doc: `docs/SHORT_URL.md`. Regression: `php scripts/verify_short_url.php`.
- **itm_qr_share_modal.php** — shared modal markup + loads `js/qrcode.min.js`, `js/itm-qr-share.js`, `js/itm-share-no-attachments-prompt.js`, `js/itm-whatsapp-share.js`, and `js/itm-outlook-share.js` (`itmOpenQrShareModal()` / `itmCloseQrShareModal()`; WhatsApp/Outlook use `itmMaybeConfirmShareNoAttachments()` when `has_images` is returned).
- **itm_whatsapp_share.php** — `itm_whatsapp_share_build_message()` and `itm_whatsapp_share_build_url()` for WhatsApp deep links (`wa.me`) using the same temporary join URL + 6-digit code as QR share. Client: `js/itm-whatsapp-share.js` (`itmOpenWhatsAppShare()`); Notes-only no-attachments confirm: `js/itm-share-no-attachments-prompt.js`.
- **itm_outlook_share.php** — `itm_outlook_share_build_subject()`, `itm_outlook_share_build_body()`, `itm_outlook_share_build_mailto_url()`, and `itm_outlook_share_build_web_compose_url()` for Outlook/mail compose using the same temporary join URL + code. Client: `js/itm-outlook-share.js` (`itmOpenOutlookShare()`).
- **itm_explorer_paths.php** — `explorer_normalize_relative_path()` and `explorer_extract_zip_safely()` shared by Explorer API/file serving.
- **itm_login_attempt_identifier.php** — `itm_normalize_login_attempt_identifier()` stores valid emails and simple usernames (`[a-zA-Z0-9_\-\.]`, max 64, not password-like); redacts secrets mistyped into the login field, including invalid `@` strings and mixed-case+digit passwords (for example `Password123`). Redacted values use `[redacted:{sha256-prefix}]` via `itm_login_attempt_identifier_redacted()`.
- **itm_ldap_auth.php** — LDAP SSO (v1): `itm_ldap_encrypt_config` / `itm_ldap_decrypt_config`, `itm_ldap_auth_attempt`, `itm_ldap_match_or_provision_employee` (match or JIT when `sso_jit_enabled`), `itm_ldap_resolve_jit_default_role_id` / `itm_ldap_resolve_jit_default_access_level_id`, `itm_sso_resolve_company_for_login`, `itm_sso_finalize_employee_login_session`, `itm_ldap_extension_available()` (`ldap_connect`). Loaded from `config.php`. Entry: `sso-ldap.php`. Doc: `docs/SSO_LDAP.md` (PHP **`ldap`** extension on Apache). Regression: `php scripts/verify_sso_ldap.php`.
- **itm_employees_auth_sensitive_fields.php** — canonical Employees list/view column denylist (`password`, `vault_key_hash`, `vault_key_escrow_encrypted`, `totp_secret`, reset-token fields); `modules/employees/index.php` merges `itm_employees_auth_sensitive_field_names()` into list `$hiddenColumns`.
- **itm_vault_master_key.php** — `itm_vault_reencrypt_password_entries()` re-encrypts all `password_entries` for one employee during vault master key change; `itm_vault_reencrypt_bookmark_urls()` re-encrypts private (`shared = 0`) `bookmarks.url`, `bookmarks.title`, `bookmarks.notes`, and `bookmark_folders.name` rows the same way; `itm_vault_reencrypt_notes()` re-encrypts private notes (`shared_with_json` empty) `title`, `content`, `checklist_json`, and `note_labels.label`; `itm_vault_reencrypt_events()` re-encrypts private events via `events_private_text_legacy_plaintext_check()` before decrypt. Caller owns transaction COMMIT/ROLLBACK. Used by `user-config.php`.
- **itm_vault_org_recovery.php** — tenant-controlled vault org recovery: company policy + escrow key, employee consent/escrow sync, admin recovery request workflow. Loaded from `config.php`. Doc: `docs/VAULT.md` §2.E. Regression: `php scripts/verify_vault_org_recovery.php`.
- **itm_totp_helpers.php** — `PHPGangsta_GoogleAuthenticator` (RFC 6238), `itm_totp_encrypt_secret()` / `itm_totp_decrypt_secret()`, `itm_totp_verify_employee_code()`, setup QR helpers; loaded from `config/config.php`.
- **itm_vault_unlock.php** — shared vault lock screen + unlock POST (`itm_vault_handle_unlock_requests()`, `itm_vault_render_lock_screen()`); master key + optional TOTP when `employees.totp_enabled = 1`. CSRF failures set lock-screen error text (no `die()` before HTML).
- **itm_user_config_stats.php** — consolidated employee dashboard COUNT stats for `dashboard.php` via `includes/itm_employee_dashboard.php` (`itm_user_config_stat_definitions()`, `itm_user_config_fetch_stats_batch()` / `_loop()`, `itm_user_config_extract_alerts_events_counts()`); `floor_plans` uses `created_by`. Benchmarks: `scripts/benchmark_stats_optimized.php`, `scripts/benchmark_user_config.php`.
- **itm_employee_dashboard.php** — `itm_employee_dashboard_load_context()` loads employee stat-card data (tickets, vault, system access auto-insert, my activity count, private module counts, dynamic module stats); used by `dashboard.php` and `admin.php`.
- **itm_employee_dashboard_cards.php** — grouped stat-card sections (`My work`, `Personal`, `Private`, `Activity`) for `dashboard.php` (scrollable auto-height grid); **`includes/itm_admin_dashboard_cards.php`** renders the same data on `admin.php` using stacked `stats-grid` inside `.card` sections; **`admin.php`** adds a **Company overview** card (equipment, tickets, employees, Active, Online now, On Leave) below those sections with the same markup; **My Activity** links to `modules/myactivity/`; no Sidebar Prefs card; company **Audit Logs** admin-only.
- **itm_employees_hidden_accounts.php** — DB-only `employees.is_hidden` ensure helper, visible-only SQL predicate, and UI exclusion list; hidden rows are omitted from Employees index/list and blocked on view/edit/delete/import/clear-table.
- **itm_fk_option_labels.php** — formats FK dropdown option text with secondary labels (`itm_format_fk_option_label_with_secondary()`, department/location/supplier/rack/manager helpers); used by Employees and Equipment create/edit selects.
- **itm_command_palette_search.php** — global command-palette unified search (phase 1 SQL `LIKE` per module: employees, equipment, tickets, ip_addresses, catalogs); **module navigation** matches all accessible sidebar/registry modules (e.g. `hotel` → `hotel_bookings`) with links to `modules/{slug}/index.php` via `itm_command_palette_sidebar_visible_module_slugs()` + registry union; RBAC via `has_module_access()` + `itm_sidebar_item_passes_role_view()`; employees admin-only; API `modules/search/api.php`; UI `js/command-palette.js` + header Ctrl+K.
- **itm_search_index.php** — phase 2 denormalized `search_index` FULLTEXT rows; CRUD sync helpers (`itm_search_index_after_module_save/delete/clear`); palette uses index when populated, SQL `LIKE` fallback when empty/no hits; backfill [apply_search_index_backfill.php?run=1&apply=1](http://localhost/it-management/scripts/apply_search_index_backfill.php?run=1&apply=1). **No Add sample data** — not seeded via `itm_seed_table_from_database_sql()` or `db/02_data_sample.sql`.
- **itm_equipment_search.php** — equipment index list search JOIN + WHERE helpers matching scalar fields (dates, purchase cost, workstation/switch/printer columns, `active`, `photo_filename`) and create/edit FK labels (department code, supplier code, location code, rack code, assignee username, type/manufacturer/status, switch/workstation lookup names).
- **itm_employees_search.php** — Employees index list search helper; extends raw column LIKE with FK label JOIN columns (status, role, department name/code, location code, position name/description, manager name/username, etc.); includes `CONCAT(first_name, ' ', last_name)` when both name columns are visible; always searches `mobile_phone` even when hidden from list columns (ESA matrix).
- **itm_crud_fk_label_search.php** — shared flattened CRUD search helper; adds tenant-scoped `EXISTS` predicates so list search matches FK label tables (`itm_crud_fk_label_search_conditions()`). Wired into scaffold `modules/*/index.php` search blocks (except bespoke modules without search).
- **itm_crud_scalar_column_search.php** — bespoke/scalar list search helper (`itm_crud_scalar_column_search_conditions()`); builds `LIKE` / `DATE_FORMAT` OR fragments for visible text and datetime columns with no FK labels (e.g. `modules/visitors_access_log/`). Recognized by `scripts/check_fk_label_search_coverage.php`.
- **itm_todo_search.php** — Todo index search helper; extends title/description LIKE with category, department, and assignee label EXISTS for CSV `*_id` columns (`itm_todo_build_search_clause()`).
- **itm_employees_delete_dependencies.php** — `itm_employees_detach_delete_dependencies()` clears or nulls inbound links (`attempts`, `audit_logs`, `employee_companies`, `employee_system_access`, passwords/bookmarks, approvers, tickets reassignment, etc.) inside the same transaction as admin employee soft-delete; used by `modules/employees/delete_functions.php`.
- **itm_employee_employment_status.php** — login/dropdown eligibility via `employment_status_id` → `employee_statuses.name` = Active (`itm_employee_active_employment_status_join_sql()`, `itm_employee_employment_status_predicate_by_name_sql()`, `itm_employee_active_employment_status_predicate_sql()`, `itm_employee_on_leave_employment_status_predicate_sql()`, `itm_employee_resolve_employment_status_id_by_name()`, `itm_employee_count_by_employment_status_name()` for **admin.php** HR cards — counts live rows only (`deleted_at IS NULL`), `itm_employee_has_active_employment_status()`, `itm_employee_resolve_active_status_id()` for `register.php`). Replaces deprecated `employees.active`.
- **itm_employee_contact_email.php** — `itm_employee_has_contact_email()`, `itm_employee_validate_contact_email_or_error()`, `itm_employee_resolve_contact_emails_after_merge()`; at least one of `work_email` / `personal_email` required on employee create/edit/import (neither field individually required in UI). Loaded from `config/config.php`.
- **itm_active_sessions.php** — **admin.php** **Online now** metric: writable presence touch files under `sys_get_temp_dir()/itm-session-presence/{company_id}/{employee_id}.presence`; `itm_active_sessions_touch()` called from `config/config.php`; `itm_count_logged_in_users_for_company()` counts recent touches within `session.gc_maxlifetime`.
- **itm_api_rate_limit.php** — tier hourly limits; **Free** = unlimited, **no API key**, **session required** (`itm_api_resolve_rate_limit_row()` reads `$_SESSION['company_id']` + `$_SESSION['employee_id']` when no key); `itm_api_tier_requires_api_key()`; probe payload `itm_api_build_rate_limit_probe_payload()` (`employee_id`, `api_key_required`); `itm_api_enforce_rate_limit_or_exit()` for programmatic endpoints.
- **itm_api_v2.php** / **itm_api_v2_scopes.php** / **itm_api_v2_openapi.php** — partner JSON REST gateway (`modules/api_v2/router.php`): PATH_INFO routing, paid-tier `X-API-Key`, scoped grants on `api_key_scopes`, RBAC via key-owner session bootstrap, OpenAPI builder. Handlers: `itm_api_v2_handlers/tickets.php`, `equipment.php`. Doc: `docs/API_V2.md`.
- **itm_password_reset.php** — `itm_password_reset_find_user_by_identifier()` (email or username), `itm_password_reset_store_token_for_employee()` (`DATE_ADD(NOW(), INTERVAL 24 HOUR)` expiry via `itm_password_reset_token_ttl_hours()`), `itm_password_reset_lookup_employee_by_token()` (hash + legacy plain `reset_token` fallback), `itm_password_reset_complete_for_employee()`; loaded from `config/config.php`.
- **itm_email.php** — tenant SMTP profiles (`email_smtp_configurations`), send log (`emails`), alert rules (`email_alert_rules`); `itm_send_email()`, `itm_email_get_default_smtp_config()`, `itm_email_dispatch_to_rule()`; optional `from_email` / `from_name` / `log_from_email` overrides on `itm_send_email()` (SMTP transport still uses tenant profile credentials; From header may differ — e.g. hotel `reservations_email`); `itm_email_build_transactional_html()` / `itm_email_apply_transactional_template()` wrap fragment HTML in login-style purple card layout (auto-applied unless `email_template => false` or body is already a full document); `itm_email_brand_name_for_template()` strips a leading gear from `app_name` before the template adds one in the header; loaded from `config/config.php`.
- **itm_inbound_email_tickets.php** — IMAP poll → ticket create/comment (`includes/itm_inbound_email_tickets.php`); routes mail to `companies.email`; dedupe `ticket_inbound_email_messages`; runner `scripts/run_inbound_email_tickets.php` requires PHP **`imap`** CLI extension.
- **itm_registration_invitation_email.php** — `itm_registration_invitation_send_email()` and `itm_registration_invitation_notify_after_save()` for Registration Invitations create/edit when `active = 1`; builds pre-filled `register.php` URLs and sends via `itm_send_email()`.
- **itm_role_assignment_rights.php** — `itm_get_assignable_role_ids()` and `itm_can_assign_role()` helpers for enforcing role assignment registry in user management modules.
- **itm_crud_browser_title.php** — `itm_crud_apply_module_icon_to_browser_title()` prepends `itm_resolve_module_sidebar_icon()` to `$crud_title` before canonical `<title>` output; bulk inject via `scripts/apply_crud_browser_title_module_icon.php`; gate `scripts/check_crud_browser_title_module_icon.php`.
- **itm_company_module_access.php** — `has_module_access()`, `get_company_modules()`, `itm_list_all_modules_registry()`, `itm_ensure_registry_rows_for_module_slugs()`, `itm_ensure_company_module_access_for_company()` (auto-backfills missing `company_module_access` rows as `enabled=1` once per company per request before sidebar prefetch), `itm_has_module_access_bust_cache()`, `itm_sidebar_discovery_probe_cleanup()`, `itm_enforce_module_access_or_exit()`, registry sync/seed helpers; `itm_module_access_always_allowed_slugs()` includes `settings`, `notifications` (header bell JSON/SSE API), and `employee_notifications` (personal inbox); loaded from `config/config.php`. `has_module_access()` prefetches all `company_module_access` rows for the active company once per request (optional fourth arg `$forceRefresh`); `itm_set_company_module_access()` and registry inserts via `itm_ensure_registry_rows_for_module_slugs()` bust that cache after writes. Non-admin users without an `ALL` wildcard on `role_module_permissions` inherit only explicit `can_view` module rows (demo1–demo5 / Helpdesk pattern); admin-only registry slugs (`audit_logs`, etc.) may open when RBAC grants `can_view`.
- **itm_user_forbidden.php** — `itm_forbidden_user_message()`, `itm_request_expects_json_response()`, `itm_exit_forbidden()`; used by RBAC (`itm_require_role_module_permission`), company module access (`itm_enforce_module_access_or_exit`), and `itm_require_admin()`. Browser fetch → JSON `{ok:false,error}`; browser form/navigation → session `itm_forbidden_message` + redirect + `itmShowForbiddenModal()`; CLI → plain text for regression probes.
- **itm_role_module_permissions.php** — `itm_resolve_active_company_id()`, `itm_user_has_role_module_permission()`, `itm_require_role_module_permission()` for server-side RBAC checks against `role_module_permissions`; `itm_crud_rbac_exempt_module_slugs()`, `itm_resolve_rbac_module_name_for_slug()`, `itm_require_crud_role_module_permission()` for flattened CRUD index handlers (create/edit/delete); `itm_mysqli_stmt_fetch_assoc()` and `itm_mysqli_stmt_fetch_all_assoc()` fall back to `bind_result` when `mysqli_stmt_get_result` is unavailable and log `mysqli_stmt_store_result` / `mysqli_stmt_result_metadata` failures via `itm_mysqli_log_stmt_fallback_failure()` (context label, optional `errno`, correlation id — no raw `mysqli_stmt_error()` text). Static audit: `php scripts/check_crud_rbac_coverage.php`. Loaded from `config/config.php`.
- **itm_company_session.php** — `itm_list_employee_accessible_companies()` (home tenant + `employee_companies` grants, or all active companies for Admin), `itm_try_auto_select_single_company_session()` (auto-picks when exactly one tenant is available), `itm_employee_has_company_access()`, `itm_switch_active_company_session()`, `itm_resolve_company_context_employee_id()`, `itm_apply_company_context_employee_session()`, and `itm_company_session_login_employee_id()` for dashboard/index company switcher and **Admin first login** (`login.php`): updates `$_SESSION['company_id']` / `company_name` and, for cross-tenant **Admin** context, maps session `employee_id` / `username` / `email` to that tenant's seed Admin (`Admin2` … `Admin5`) while `login_employee_id` stays the authenticated user. Clears `vault_key` when the context employee changes. `login.php` and `index.php` skip the company picker when `itm_try_auto_select_single_company_session()` succeeds. Loaded from `config/config.php`.
- **ui_config.php** — sidebar discovery; `itm_sidebar_base_structure()` defines **`budgeting`** (forecast, GL, budget report, `approver_*` workflow) and **`finance`** (`💳 Finance` — `expenses`, `bills`, `invoices`, `customers`, `bank_accounts`, `suppliers`, `tax_rates`, paid/payment lookups, `integration_accounts`); `itm_get_employee_sidebar_preferences_config()` falls back to tenant seed admin / legacy `employee_id = 1` rows when the signed-in employee has no layout; `itm_reconcile_employee_sidebar_preferences_canonical_sections()` fixes stale `section_id` on load; `itm_normalize_sidebar_submenu_order()` re-buckets items to `itm_sidebar_default_item_parent_map()` (not persisted wrong sections); `itm_sidebar_section_effective_visible()` / `itm_sidebar_sync_section_visibility_from_items()` gate section headers; `itm_sidebar_item_effective_visible()` centralizes live sidebar visibility (prefs, `has_module_access`, audit/equipment gates, **`employee_roles.sidebar_show`** via `itm_employee_role_sidebar_show_enabled()` / `itm_equipment_type_sidebar_effective_visible()`); `itm_sidebar_item_layout_visible()` returns saved layout prefs for **user-config.php** Personalized Sidebar checkboxes (no role override); `itm_sidebar_apply_access_gates_to_visibility()` forces inaccessible modules hidden on save; `itm_user_config_save_personalized_sidebar_items()` persists Personalized Sidebar checkboxes to **`employee_sidebar_preferences`**; `itm_save_ui_configuration()` fails when sidebar prefs sync fails; `includes/sidebar.php` re-runs `itm_normalize_sidebar_submenu_order()` at render so new catalog modules append automatically; `itm_sidebar_base_structure()` **Admin** section includes `system_status` (`🖥️ System Status`) and `backup_tape_log` (`📼 Backup Tape Log File`); Employee section exposes `employees` as `👤 Employees` (one catalog id only — duplicate `employees` entries overwrite labels); standard CRUD materialization (`itm_materialize_standard_crud_module_files()`, `itm_auto_create_module_scaffold()`); ⚠️ prefix for newly scaffolded tables (`itm_sidebar_auto_scaffolded_module_emoji()`); `itm_sidebar_module_is_hidden()` skips sidebar/registry auto-discovery for internal tables (`password_entries`, `password_folders`, floor-plan support tables, and any `*_share_sessions` QR share tables); legacy delegate detection (`itm_module_php_file_delegates_to_manufacturers_module()`); QA cleanup (`itm_remove_standard_crud_scaffold_module_dirs()`); `itm_ensure_ui_configuration_table()` / `itm_ensure_employee_sidebar_preferences_table()` cache successful schema-ensure per request (skip when `$report` is passed for Settings diagnostics); favicon helpers `itm_ui_config_resolve_favicon_relative_path()` / `itm_ui_config_favicon_url($config, $company_id)` fall back to `images/favicons/company_{id}.ico` when DB path is empty but the file exists; Settings calls `itm_ui_config_sync_favicon_path_from_disk()` on load to persist the canonical path; list-header create placement uses `itm_resolve_new_button_position($ui_config)` (default **`left`** via `itm_ui_config_defaults()` — do not duplicate `left_right` fallbacks in modules).
- **itm_date_format.php** — UK dd/mm/yyyy parse/display helpers (`itm_parse_date_input`, `itm_format_date_display`, `itm_date_input_iso_value` for HTML5 `type="date"` values, `itm_format_cell_scalar_display`); hospitality stay dates use **`itm_is_hospitality_date_field_name()`** + **`itm_format_hotel_date_display()`** → `d/M/Y` (e.g. `31/Aug/2026`, `01/Oct/2026`); audit gate **`php scripts/check_date_format.php`** (project-wide) and **`php scripts/check_hospitality_date_format.php`** (hospitality subset); audit stamp layout `itm_format_audit_timestamp_display()` → `d-m-Y - H:i:s`; loaded from `config/config.php`. Excel import uses `itm_normalize_sql_date_literal()` in `config.php`.
- **itm_hotel_date_input.php** — `itm_render_hotel_date_input()` (text + hidden native date + 📅 picker); paired with `js/hotel-date-input.js` (`window.itmHotelDateFormatYmd`); loaded from `config/config.php`. Do not nest the widget inside a caption `<label>` without `for=` — that blocks 📅 `showPicker()`.
- **itm_crud_audit_fields.php** — scaffold soft-delete + audit meta UI helpers (`itm_crud_is_list_hidden_audit_field`, `itm_crud_append_not_deleted_predicate`, `itm_crud_build_soft_delete_sql` also sets `active=0` and always inserts a space before the trimmed `WHERE` clause so `active=0WHERE` cannot form, `itm_crud_render_form_hidden_active_input` / `itm_crud_force_active_live` for status-driven modules, `itm_crud_render_status_label_badge` for list/view status FK badges, **`itm_crud_register_column_type_map()`** + **`itm_crud_render_checkbox_boolean_cell_value()`** (non-`active` tinyint(1) list/view cells → ✅/❌; hospitality scaffold modules register column types on load), create/update stamps, `itm_crud_normalize_bind_values_for_persist()` to coerce empty audit/date columns to SQL `NULL` before prepared-statement bind, view/form/delete hidden renderers); slug list loader for `docs/list_soft-delete.txt`. `itm_crud_stamp_create_audit()` sets `deleted_by`/`deleted_at` to PHP `null` (not `''`) and stamps `updated_by`/`updated_at` on create. Loaded from `config/config.php`.
- **employee_profile_photo.php** — employee profile photo paths under `files/{company_id}/Private/{username}_{employee_id}/profile/` (legacy `{username}_{user_id}` still served when `employee_id` is set); upload (`emp_profile_photo_store_upload`), serve URL (`emp_profile_photo_url` → **app-absolute** `BASE_URL/modules/explorer/file.php?path=…`, never module-relative `../../modules/…`), birthday display (`emp_format_birthday_display`, `emp_format_birthday_day_only`). Used by `user-config.php`, `modules/employees/`, and `modules/birthdays/`.
- **itm_profile_photo_upload.php** — shared PNG/JPG resolver (`itm_profile_photo_allowed_extension`) using `getimagesize()`, finfo MIME, browser `type`, and filename fallback; used by `modules/private_contacts/includes/private_contact_photo.php`.
- **fk_dropdown_helpers.php** — tenant FK label resolution (`itm_fk_label_column_for_table()` prefers `name_type` for `employee_type`); **`ui_configuration`** rows resolve to employee full name + API tier via `itm_fk_ui_configuration_label_by_id()` / `itm_fk_ui_configuration_options()`; business-key remap via `itm_fk_resolve_company_equivalent_id()`.
- **itm_system_status_native.php** — PHP/MySQL + Linux hardware JSON for `scripts/system_status_api.php` (`itm_system_status_native_payload()`, `/proc` + mysqli).
- **itm_system_status_cache.php** — `system_status` table cache get/save/refresh (`itm_system_status_cache_get($conn, $tabKey, $companyId)`, `itm_system_status_refresh_all()`); collectors for Monitoring, PHP Settings, and Database tab payloads; `cache_get()` scopes by `company_id`, uses `itm_mysqli_stmt_fetch_assoc()` (mysqlnd fallback), and routes execute failures through `itm_system_status_safe_stmt_execute()` (`errno` + correlation id in `error_log()`, no raw `mysqli_stmt_error()` text). `cache_save()` logs `json_encode` failures with `tab_key` and `company_id`, and explicitly populates the `created_by` and `updated_by` columns from the active session `employee_id`.
- **itm_system_status_powershell.php** — Windows hardware runner: `shell_exec` availability, `.ps1` readability, `itm_system_status_run_powershell_action()` (hardware-only action allowlist + `[a-z0-9_]+` guard before loading `includes/{action}.ps1`; PHP/MySQL actions stay native).
- **mysql_status.ps1** — optional Windows SCM probe for `scripts/test_mysql_status.php` (not used by live API refresh). Resolves `mysql` / `MariaDB` / `MySQL80` services, then falls back to `mysqld` process or TCP `127.0.0.1:3307` / `:3306` when Dunebox/Laragon runs mysqld without a matching Windows service name.
- **itm_system_status_storage.php** — Explorer/upload directory sizes and active-database table metrics for System Status tabs; parent nodes with children sum child totals plus direct files (`itm_system_status_directory_direct_metrics()`); skips full recursive scan when children exist; both direct and recursive metrics require `is_readable()` before iterating; excludes `.htaccess`, `index.html`, and `AGENT_NOTES.md` from file/byte totals (`itm_system_status_is_ignored_storage_file()`); department/user/DB report queries use `itm_mysqli_stmt_fetch_all_assoc()` (mysqlnd fallback) with `itm_system_status_safe_stmt_execute()` guards; shared helpers `itm_system_status_make_correlation_id()` and `itm_system_status_safe_stmt_execute()`.
- **bootstrap_helpers.php** — upload directory hardening (`itm_ensure_upload_directory*`, `itm_upload_directory_policy_body()`); schema introspection helpers `itm_table_has_column()` and `itm_table_column_is_nullable()` cache `information_schema` keys case-insensitively (`strtolower`) and treat missing nullable columns as non-nullable (`array_key_exists`, not `!empty()`). Regression: `php scripts/verify_metadata_column_cache.php`.
- **itm_myactivity.php** — employee-scoped audit activity helpers for `modules/myactivity/` (`myactivity_build_query()`, `myactivity_build_search_conditions()`, `myactivity_private_audit_exempt_labels()`, `myactivity_resolve_module_href()`, payload preview helpers); read-only consumer of `audit_logs` filtered by session `employee_id`.
- **itm_news_feed.php** — multi-source news/RSS feed cache/API helpers for `modules/news/` (`news_feed_source_catalog()`, `news_microsoft_support_atom_url()`, `news_microsoft_support_feed_catalog_entries()`, `news_build_nvd_api_query_params()`, `news_update_cache()`, `news_handle_feed_request()`, `news_ensure_cache_for_ui()`); NVD source uses `pubStartDate`/`pubEndDate` (120-day window, not bare `startIndex=0`); Microsoft Support product Atom feeds from `itm_news_feed_ms_support_products.php` (205 products, feed picker); per-source cache under `modules/news/cache/`; RBAC-exempt read-only module; loaded via `modules/news/news_feed_bootstrap.php`.
- **database_sql_unique_audit.php** — parses `db/` CREATE TABLE blocks for tenant unique-key policy; pass when PRIMARY KEY plus any business `UNIQUE` led by `company_id` (not only the resolved scope column — e.g. `nominal_code`, `document_number`, `rate_slug`). `employee_companies` scope column is `employee_id`. Exempt skips include junction/append-only tables (`live_chat_participants`, `hotel_bookings`, `hotel_booking_distribution_ari_events`, `hotel_booking_distribution_webhook_queue`, finance allocations, photo children, etc.). Used by `php scripts/check_database_sql_company_name_uniques.php`.

## 8. Multi-Tenant Rules
- Visibility helpers always take `company_id` / user context from caller; never bypass tenant filters in shared helpers.

## 10. Common Pitfalls

- Never skip shared visibility/search helpers (`alerts_visibility`, notes/todo filters, FK label search). [Cursor-Valid]
- Table/column identifiers must pass `itm_is_safe_identifier()` — never concatenate untrusted names into SQL. [Cursor-Valid]
- Upload/tenant file trees: never bare `mkdir()` — use `itm_ensure_upload_directory*` / `itm_ensure_files_storage_directory()`. [Cursor-Valid]
- Do not set `ITM_HTTP_ENDPOINT_CONTRACT_TEST` from HTTP input. [Cursor-Valid]

## 11. Examples of Safe Code Patterns

### Using Alerts Visibility SQL
```php
require_once ROOT_PATH . 'includes/alerts_visibility.php';
$visibility = itm_alerts_visibility_sql('alias');
```

## 12. Module Owner Notes (Optional)
Centralized logic here prevents code duplication and ensures consistent security/visibility enforcement.
