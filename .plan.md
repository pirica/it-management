1. *Modify `modules/private_contacts/edit_form.php` to enforce PNG-only uploads, add confirmation logic, and update paths.*
   - Set `accept=".png"` on the file input.
   - Add a hidden input `confirm_replace`.
   - Update JavaScript to:
     - Validate the `.png` extension.
     - Show a confirmation dialog if a photo already exists.
     - Set `confirm_replace` to 1 if confirmed.
   - Update the `<img>` src paths to: `../../files/<?php echo $_SESSION['company_id']; ?>/Private/<?php echo $_SESSION['username'] . "_" . $_SESSION['user_id']; ?>/private_contacts/<?php echo htmlspecialchars($contact['photo']); ?>`.
2. *Modify `modules/private_contacts/create.php` and `modules/private_contacts/edit.php` to implement paths, PNG enforcement, debugging, and auto-creation.*
   - Define directory: `$dir = "../../files/$companyId/Private/{$username}_{$userId}/private_contacts";`.
   - Implement the user's requested debugging and directory creation logic:
     - `echo` DIR and REALPATH.
     - Recursive `mkdir($dir, 0777, true)` with error reporting.
     - Explicit `chmod($dir, 0777)` as requested.
     - `error_log` creation attempts.
   - Enforce PNG extension and MIME type validation.
   - In `edit.php`, check the `confirm_replace` flag before overwriting.
3. *Update `modules/private_contacts/index.php` and `modules/private_contacts/view.php` to match the standardized path.*
   - Ensure `<img>` tags use `../../files/{company_id}/Private/{username}_{user_id}/private_contacts/`.
4. *Verify the logic and syntax.*
   - Use `read_file` to confirm the implementation of debugging, paths, and permissions in all modified files.
   - Run `php -l` on all modified PHP files.
5. *Complete pre commit steps*
   - Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
6. *Final verification and Submission.*
   - Run `php scripts/run_tests.php` and `bash scripts/smoke_test.sh`.
   - Perform a manual test upload to verify the debug output and file structure in the shell.
   - Submit the changes with a descriptive commit message.
