## Summary

- Add centralized per-request schema caches in `includes/itm_crud_schema_helpers.php` (`itm_crud_table_columns`, `itm_crud_fk_map`, `itm_crud_table_column_meta`) and wire them from `config/config.php`.
- Tier 1: replace duplicate uncached probes in shared `includes/` (audit, FK risk, software EOL, sample seed, company module access, employee system access, UI config, budget report, etc.) with cached helpers.
- Tier 2: bulk-delegate scaffold `cr_table_columns` / `cr_fk_map` / `cr_fk_metadata` across flattened CRUD modules (~643 files) via `scripts/apply_crud_schema_cached_helpers.php`; static gate `scripts/check_crud_schema_cached_helpers.php`.
- Tier 3: IDF API bootstrap, employees/settings/onboarding modules, and related paths use `itm_table_has_column()` / `itm_crud_table_columns()` instead of raw `SHOW COLUMNS` / inline `DESCRIBE`.

## Test plan

- [x] Smoke equivalent (Windows PowerShell): PHP lint on 2649 files — OK
- [x] `php scripts/check_csrf_coverage.php` — pass
- [x] `php scripts/check_sql_injection_coverage.php` — pass
- [x] `php scripts/check_fk_label_search_coverage.php` — pass
- [x] `php scripts/check_crud_schema_cached_helpers.php` — pass
- [x] Database import (PowerShell pipe of `db/01_schema.sql` + `02_data.sql` + `03_triggers.sql` to MySQL 3307, password from `.env`) — 248 tables
- [x] `php scripts/verify_database_schema.php` — pass (248/248)
- [x] `php scripts/verify_crud_fk_label_search.php` — pass
- [x] `php scripts/run_tier2_checks.php` — pass (33/33)
- [x] `ITM_SKIP_DB_TESTS=1 php scripts/run_tests.php` — OK (1031 tests, 1746 assertions, 160 skipped DB integration)
- [x] `php scripts/verify_metadata_column_cache.php` — pass

Note: `bash scripts/smoke_test.sh` and `bash scripts/verify_database_sql_import.sh` are not available locally (WSL/bash missing); equivalent commands above were run with Dunebox PHP/MySQL paths.
