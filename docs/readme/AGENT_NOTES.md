# AGENT_NOTES.md - docs/readme

## 1. Module Purpose
Supplementary readme documentation assets (not the root `README.md`), including architecture diagrams referenced from root `README.md`.

## 7. File Structure
- `company-module-access-architecture.svg` — Company Module Access Management architectural map (UI surfaces, request bootstrap, database tables).
- `roles_permissions.png` — Roles & Permissions dashboard screenshot (Features / Login and Screenshots sections in root `README.md`).
- `license_management.png` — License Management module list screenshot (Screenshots section in root `README.md`).
- `demo_license_management.png` — License Management demo screenshot (Features / Login section in root `README.md`).
- `demo_emails.png` — Email Management demo screenshot (Send Logs tab, stat cards, SMTP/Alert Rules tabs; Features / Login section in root `README.md`).
- `booking_portal.png` — Public hotel booking portal home (`booking/index.php`; Features / Login and Screenshots sections in root `README.md`).
- `hotel_bookings.png` — Hotel Bookings admin list (`modules/hotel_bookings/`).
- `hotel_booking_hotels.png` — Hotels admin list (`modules/hotel_booking_hotels/`).
- `hotel_booking_settings.png` — Hotel Booking Settings admin (`modules/hotel_booking_settings/`).
- `booking_rooms_types.png` — Room Types admin list (`modules/booking_rooms_types/`).
- `hotel_booking_portal_rate_plans.png` — Portal Rate Plans admin list (`modules/hotel_booking_portal_rate_plans/`).
- `hotel_bookings_present.png` — Present booking segment status admin (`modules/hotel_bookings_present/`).

## 10. Common Pitfalls

- README screenshots under `docs/readme/` are generated by `scripts/take_screenshots_modules.py` for signed-in module pages — do not hand-edit those PNGs when the script can regenerate them. The public **`booking/`** portal is not an `modules/` slug; capture `booking_portal.png` with Playwright against `http://localhost/it-management/booking/` (no Admin session). [Cursor-Valid]

## 12. Module Owner Notes (Optional)
Project entry docs: root `README.md`, authoritative agent rules in `AGENTS.md`, scripts standards in `scripts/SCRIPTS.md`, hotel booking reference `docs/BOOKING.md`.
